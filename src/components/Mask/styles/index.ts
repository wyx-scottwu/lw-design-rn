import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    width: 100,
    height: 100,
    overflow: 'hidden',
    borderColor: 'red',
    borderWidth: 1,
  },absContainer: {
    position: 'absolute',
    top: 400,
    left: 160,
    width: 100,
    height: 100,
    overflow: 'hidden',
    borderColor: 'red',
    borderWidth: 1,
  },
})